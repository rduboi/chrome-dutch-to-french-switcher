{
    "name": "Dutch to french url switcher",
    "version": "0.0.1",
    "description": "Dutch to french url switcher will force the the current website to use french instead of dutch by rewriting the url when possible.",
    "permissions": [
        "tabs"
    ],
    "manifest_version": 2,
    "background": {
        "scripts": [
            "background.js"
        ],
        "persistent": false
    }
}